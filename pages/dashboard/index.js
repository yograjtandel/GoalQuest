import {
  Grid,
  GridItem,
  Box,
  Input,
  Button,
  Checkbox,
  Flex,
  Select,
} from "@chakra-ui/react";
import { AddIcon, ChevronDownIcon } from "@chakra-ui/icons";

const setting = () => {
  return <Box p={4} w={"100%"} display={"flex"} justifyContent={"start"}></Box>;
};

export default setting;
